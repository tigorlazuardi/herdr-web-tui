---
paths: "frontend/**"
---

# Browser UI testing

Use `browser_qa` for responsive desktop/tablet/mobile UI/UX smoke tests. Use `browser_snapshot` plus `browser_*` interactions for targeted flows; prefer accessibility refs. Check screenshots, console, page errors, network failures, and Web Vitals before declaring frontend UI work done.
